now when I do the registration there is no email sent to email address that users are writing. Now I want it when the user is writing his email and signs in to the presentation the email is going to be sent for the address that they write. it should send the email following the template that I have in `email_template.html`.
1. check if all my libraries are installed so it actually runs
2. make it so it still excepts the user with the same email but with different name and last name
3. and make it so it sends the email.